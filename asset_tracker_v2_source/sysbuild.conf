# Asset Tracker v2 + SoftSIM Sysbuild Configuration

# Enable SoftSIM template bundle for automatic profile template generation  
# Note: This may fail if SoftSIM module is not loaded - we can do manual template generation instead
# SB_CONFIG_SOFTSIM_BUNDLE_TEMPLATE_HEX=y